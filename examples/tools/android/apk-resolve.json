{
  "name": "apk-resolve",
  "display_name": "APK Resolve",
  "type": "python_script",
  "path": "apk-resolve/apk_resolve.py",
  "env_deps": ["python"],
  "parse_stdout_json": true,
  "inputs": [
    {
      "name": "apk_path",
      "type": { "base": "text" },
      "required": true,
      "description": "Local file path or http(s) URL of the APK to resolve."
    }
  ],
  "outputs": [
    {
      "name": "apk_path",
      "type": { "base": "text" },
      "required": true,
      "description": "Absolute path to the resolved APK (downloaded to apk_cache when a URL is given)."
    }
  ],
  "operations": [
    {
      "name": "resolve",
      "description": "Resolve a local path or http(s) URL to an absolute local APK path, downloading into <cwd>/apk_cache when the input is a URL.",
      "inputs": [
        {
          "name": "apk_path",
          "type": { "base": "text" },
          "required": true,
          "description": "Local file path or http(s) URL of the APK."
        }
      ],
      "outputs": [
        {
          "name": "apk_path",
          "type": { "base": "text" },
          "required": true,
          "description": "Absolute path to the resolved APK."
        }
      ],
      "args_map": [
        { "param": "apk_path" }
      ]
    }
  ]
}

{
  "name": "release-evidence",
  "version": "1.0",
  "description": "Sub-workflow of release-check-item: write one evidence file per audited file into the run's artifact directory (${rundir} interpolation plus the basename pipe) — builtin-only",
  "inputs": [
    {"name": "path", "type": {"base": "text"}, "required": true, "description": "Audited file"},
    {"name": "size", "type": {"base": "number"}, "required": true, "description": "File size in bytes"},
    {"name": "todos", "type": {"base": "number"}, "required": true, "description": "TODO/FIXME marker count"},
    {"name": "debug", "type": {"base": "number"}, "required": true, "description": "Leftover debug statement count"}
  ],
  "outputs": [
    {"name": "passed", "type": {"base": "boolean"}, "required": true, "description": "True once the evidence file is written"}
  ],
  "nodes": [
    {
      "id": "write",
      "tool": "file.write",
      "params": {
        "path": "${rundir}/evidence-${inputs.path | basename}.txt",
        "content": "file: ${inputs.path}\nbytes: ${inputs.size}\ntodo/fixme markers: ${inputs.todos}\ndebug statements: ${inputs.debug}\n"
      },
      "next": "confirm"
    },
    {
      "id": "confirm",
      "tool": "flow.assert",
      "params": {
        "condition": true,
        "message": "evidence written for ${inputs.path}"
      },
      "next": null
    }
  ]
}
